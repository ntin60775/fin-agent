---
node_type: ticket
title: Копилка и округление — следствия канона
service: _platform
status: archived
updated: 2026-09-26
links:
  documents: [../../../finance_core/roll.py, ../../../finance_core/README.md, ../../../CONTEXT.md, ../../decisions/deal-balance-canon.md, ../../../tests/test_roll.py, ../../../tests/test_cash.py]
  part_of: [README.md]
  depends_on: [06-deal-start-and-balance-canon.md]
---

# 07: Копилка и округление — следствия канона

**What to build:** у участника копилки то же число, что у любой сделки: остаток падает
платежами, а «закрывается разом» остаётся правилом **единицы** (единица закрыта, когда накоплено
не меньше её цели) — сегодня остаток участника намеренно стоит на полной сумме сделки, пока
единица не закрыта, и это третье расхождение «двух ответов на один вопрос»: у сделки в копилке
расчётный остаток падает, а остаток проката нет.

Плюс округление: остаток и начисление округляются тем же кодом, что прокат (копейка, половина
вверх), а прожиточный минимум окна остаётся с округлением **вверх** — это выбранная сторона
ошибки (свободные деньги занижаются, а не завышаются), и она должна быть записана словами, а не
следовать из кода.

**Решения**

- **Остаток участника падает платежами в котёл.** «Копилка закрывается разом» — свойство
  единицы, а не остатка участника: фиктивное число у сделки ломает сверку с выпиской в принципе
  и даёт третий ответ на вопрос об остатке.
  *Отклонено:* остаток участника = «цель минус котёл» (23/35) — это ответ про единицу, а не про
  сделку, и он уже есть; плоский остаток (14/35) — дефект, который чиним.
- **Единица закрывается по котлу, как и сейчас.** Правило закрытия не трогаем: платежи копятся,
  цель фиксирована, закрытие — когда накоплено не меньше цели. Меняется только число у
  участника.
  *Отклонено:* закрывать, когда закрыты все участники (24/35) — второе правило закрытия рядом с
  первым; по первому из двух (20/35) — тот же дубль плюс неопределённость.
- **Проценты внутри копилки не идут — правило остаётся.** Цель зафиксирована при договорённости и
  не растёт; начисление внутри копилки превратило бы её в долг с процентами и разошлось бы с
  договорённостью.
  *Отклонено:* начислять участникам (24/35) — меняет смысл единицы закрытия.
- **Округление остатка и начисления — как в прокате; прожиточный минимум — вверх.** Сторона
  ошибки у минимума выбрана сознательно (занизить свободные деньги, а не завысить) — это
  записывается рядом с формулой свободных денег, а не остаётся следствием кода.
  *Отклонено:* всё вверх (25/35) — занижает и остаток; всё половина вверх (26/35) — снимает
  страховку у минимума; отдельная запись в `docs/decisions/` (28/35) — документ ради одной
  строки, когда место этой строки — рядом с формулой.

**Blocked by:** 06 — канон остатка.

- [x] Синтетика: платежи в котёл уменьшают остаток участника; расчётный остаток участника и остаток проката совпадают.
  Тесты `test_pot_member_balance_falls_with_pot_payments_and_equals_the_canon` и
  `test_closure_unit_is_a_piggy_bank`. Замер-свидетель: канон на открытие 17 973,70,
  остаток проката 15 333,17 → 12 639,83 → 9 892,63 — равен канону на каждую дату
  у обоих участников.
- [x] Синтетика: единица закрывается, когда накоплено не меньше цели, и тогда остатки участников обнуляются разом.
  Тест `test_closure_unit_is_a_piggy_bank`: январь — котёл 2 000 / цель 3 000, остатки
  1 772,42 и 171,65; февраль — котёл 3 000 ≥ цели, единица закрыта, остатки 0 и 0.
  Края (котёл == цели, котёл > цели, несколько участников) проверены.
- [x] Синтетика: внутри копилки проценты не начисляются (правило осталось).
  Тест `test_closure_unit_is_a_piggy_bank`: котёл ровно на платежах (2 000 и 3 000),
  цель ровно на телах (3 000 все месяцы); начисленное 128,47 числится в остатках
  участников, а не в котле.
- [x] Округление остатка и начисления совпадает с прокатом (тест на копейку), а сторона округления прожиточного минимума названа в `finance_core/README.md` и `CONTEXT.md`.
  Тест `test_pot_member_balance_and_accrual_are_kopeks_like_the_roll` (104,08 — третья
  цифра 3 вниз, 104,13 — ровно половина вверх). Сторона округления прожиточного минимума
  названа словами в `finance_core/README.md` и `CONTEXT.md` и закреплена
  `test_the_living_floor_rounds_up_so_free_money_never_overstates` и
  `test_the_floor_requirement_and_consumed_round_up_by_a_kopek` — страховка на сторону
  ошибки, нового поведения они не доказывают (`ROUND_CEILING` был и до тикета).
- [x] `python3 -m pytest tests/` зелёные.
  `306 passed`; линт KB канонической копией
  (`.omp/plugins/node_modules/ontoship/skills/kb-search/gitmark.py lint`) чист.

Независимое ревью подтвердило:

- (а) Снятое исключение «участник копилки» заслужено: равенство «прокат = канон» для
  участника выполняется — с оговоркой «пока единица открыта» (после закрытия прокат
  обнуляет остатки, а канон на ту же дату ещё числит долг); второе названное исключение
  (вхождение, которое платит сам прокат) сохранено.
- (б) Цессия внутри окна у участника копилки закрыта: цель 100 000 → 100 000 → 120 000,
  31.03 = 139 239,04 в обоих носителях; снимок состояния (повтор шага месяца) закреплён
  тестом `test_repeating_the_month_step_keeps_the_transfer_delta_once` после блокирующего
  замечания.
- (в) Вне копилок батарея из 12 блоков — 0 расхождений.
- (г) Хвост «участники закрытой копилки продолжают копить нехватку (`short`)» —
  пре-существующее поведение, вынесено в сборник хвостов (тикет 09).
