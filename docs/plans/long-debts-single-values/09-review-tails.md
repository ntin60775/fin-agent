---
node_type: ticket
title: Хвосты ревью — сборник неблокирующих замечаний
service: _platform
status: draft
updated: 2026-09-25
links:
  documents: [../../../finance_core/roll.py, ../../../finance_core/actions.py, ../../../finance_core/forecast.py, ../../../finance_core/settlements.py, ../../../tests/test_actions.py, ../../../CONTEXT.md, ../../../finance_core/README.md]
  part_of: [README.md]
  depends_on: [08-saldo-and-reconciliation.md]
---

# 09: Хвосты ревью — сборник неблокирующих замечаний

## Зачем

Сборник неблокирующих замечаний независимых ревью тикетов 01–08: хвосты
собирались по ходу тикетов 03–08, новые дописываются до старта рана, и сборник
выполняется последним, после 08. При разной природе проверки на исполнении
делится на два рана: код — прогоны, доки — линт.

Каждый пункт помечен своим источником и статусом на main на дату проверки;
закрытые вынесены одной строкой в «Уже закрыто» и в работу не идут — сейчас их
два, оба со свидетелями: тикеты 04 и 07.

## Код

**1. Пробелы и первая дата** —
Было: на книге, где ВСЕ долги — пробелы (schedule=None), `roll_deals` отдаёт
`payoff_by_graph=None` с причиной «за отведённые месяцы долг не закрылся», а
`Forecast.first_priority` отдаёт первый месяц окна без причины (freedom на
пустом наборе = первый месяц). Пара читается противоречиво: «закрыт» и
«не закрывается» рядом.
Надо: пробел — не закрытие и для первой даты (причина рядом) либо явная
формулировка, что первая дата на такой книге ничего не значит.
Источник: отчёт ревью 02, «3. Неблокирующие», п.1.
Статус на main: подтверждено — правка `and not graph.gaps` внесена
(`roll.py:602`, закреплена `tests/test_roll.py::test_a_gap_is_not_a_payoff_closed_before_the_roll`),
но разрыв остался: `payoff_by_graph = None + PAYOFF_NOT_CLOSED` (`roll.py:606-609`)
против `first_priority = месяц без причины` (`forecast.py:421-424`; freedom на
пустом наборе ставится в `roll.py:838-839`).

**2. Неиспользуемый импорт `Payment` в tests/test_actions.py** —
Было: строка 12 — `Payment` в списке импорта; импорт не используется и
существовал до плана.
Надо: убрать из импорта.
Источник: замечание оператора при заведении сборника (существовал до плана).
Статус на main: подтверждено — grep по `tests/test_actions.py` находит `Payment`
только в строке 12 (использований в файле нет).

**3. Избыточный фильтр платежей в `accrued_interest`** —
Было: `finance_core/settlements.py:723-726` отбирает платежи условием
`if since <= when <= until` при построении `by_day`.
Надо: убрать мёртвое условие — дни вне отрезка всё равно не посещаются циклом
(`settlements.py:737-738`), а `paid` фильтруется по `from_day`/`till_day`
(`settlements.py:750-751`), то есть фильтр поведенчески эквивалентен своему
отсутствию. Вместе с удалением условия добавить тест, где платежи лежат ВНЕ
отрезка (до `since` и после `until`) и не меняют начисления.
Источник: отчёт ревью тикета 03, мутация M7 («снятие фильтра» → 0 упавших из 273).
Статус на main: подтверждено чтением кода — условие на месте
(`settlements.py:725`), цикл и `paid` ограничивают отрезок теми же границами.
Замечание к покрытию: батарея ревьюера 03 гоняла прокат через `monthly_extra`,
путь `budgets=`/`window=` у `roll_deals` покрыт только частично — если правка
этого места делается, добавить тест и на этот путь.

**4. Вхождение, которое платит сам прокат, расходится с каноном** —
Было: при `Deal.start` внутри месяца и вхождении с датой РАНЬШЕ начала долга в
том же месяце прокат начисляет на базе до применения платежей месяца, а канон
вычитает платежи до `since`. Факты ревью: `start=31.01`, платёж 1 000 от 20.01 →
прокат 99 064,52 против канона 99 063,87 (+0,65; далее +0,66, +0,68); с дневной
ставкой и платежом 50 000 → 50 100,00 против 50 050,00 (+50,00; далее канон
уходит в −52,35 при прокате 0,00). Причина структурная: сумма платежа к моменту
начисления ещё неизвестна (её могут урезать), поэтому исправлено только то, что
уже лежит в книге (движения).
Надо: либо вычитать из базы отрезка строки `rows` с датой < `since`, либо явно
сузить заявление «прокат = канон» (`settlements.py:694-699`,
`settlements.py:762-769`) и назвать этот случай исключением.
Источник: ревью тикета 06, N1 и дельта-ревью п.3.
Статус на main: подтверждено (числа и выводы — отчёт ревью 06; код в main
после merge `697274a`, тикет 06 archived) — каноническая сторона вычитает
движения до `since` (`settlements.py:709-711`, `723-726`), обе стороны считает
одна `accrued_interest` (`settlements.py:760`).

**5. Дельта цессии в середине месяца входит в базу годового начисления всего месяца** —
Было: цессия 25.03 даёт те же +400,00, что и 01.03, тогда как посуточно с 25.03
было бы +90,32. Правило записано в докстринге `accrued_interest`
(`settlements.py:762-769`) и в решении (вариант 11,
`docs/decisions/deal-balance-canon.md:97-101`), оба носителя равны
(прокат = канон), но теста на «середину месяца» нет — правило незакреплено.
Надо: решить — оставить (и закрепить тестом с числом) или считать
пропорционально дням.
Источник: ревью тикета 06, дельта, N7.
Статус на main: подтверждено чтением — правило «по своей дате» есть в коде
(`settlements.py:733`) и в решении (вариант 11); в `tests/test_settlements.py`
дельта проверяется на 01.03 и до окна (`:505`, `:525`), передачи в середине
месяца с числом нет.

**6. Участники уже закрытой копилки продолжают копить «нехватку» (`short`)** —
Было: после закрытия единицы (котёл ≥ цели, остатки участников обнулены)
`short` по участникам продолжает расти — факт ревьюера: 1 000 → 2 000 → 2 000;
числа идентичны базовому коду, то есть тикет 07 этого не менял и не ухудшал
(пре-существующее поведение, вне области тикета).
Надо: решить — должна ли закрытая единица перестать показывать нехватку (её
обязательства закрыты разом) или это осознанное поведение; если осознанное —
назвать словами в доках.
Источник: ревью тикета 07, N4.
Статус на main: подтверждено фактом — прогон `/tmp/rv07/probe_afterclose.py` на
текущем main: m2 `closed=True`, `short`=1 000; m3 `short`=2 000; m4
`short`=2 000. Механика: участник упирается в `room = unit.remaining` = 0,
`amount = 0`, `short += want` (`roll.py:895-905`).

## Доки

**1. Цитата «не достигнута за окно» и новая формулировка про обе даты** —
Было: доки утверждали общую цитату «не достигнута за окно» для ступеней и дат,
а у дат фактические строки другие.
Надо: привести к фактическим строкам: у срока по графику — только фраза
`PAYOFF_NOT_CLOSED` (без причины конца окна), у срока с досрочками — та же
фраза плюс причина конца окна; «второй приоритет не закрылся за окно» и
«на подушку не хватило за окно» — отдельные фразы.
Источник: отчёт ревью 01, «3. Неблокирующие», N6.
Статус на main: формулировка изменилась — цитата «не достигнута за окно» для дат
устранена, формулы перечислены (`CONTEXT.md:268-272`,
`finance_core/README.md:327-331`); **осталось**: «плюс причина конца окна»
(CONTEXT.md) и «добавляют после неё причину конца окна» (README) приписаны ОБЕИМ
датам, а `payoff_by_graph` отдаёт фразу без суффикса (`roll.py:609`,
`forecast.py:408-409`; факт закреплён
`tests/test_forecast.py::test_both_dates_say_not_closed_with_one_phrasing`),
причина конца окна добавляется только у `first_priority` (`forecast.py:423`).

**2. Граница точности `_money` на остатках ≥ 1e25** —
Было: произведение «остаток × ставка» округляется 28-значным контекстом Decimal
ДО `_money`, поэтому на остатках ≥ 1e25 копейки уже условны.
Надо: решить — фиксируем ли это ограничение словами в `finance_core/README.md`
(не дефект, а названная граница; решение записать в пункт приёмки).
Источник: отчёт ревью 02, «5. Вердикт по `_money()`».
Статус на main: подтверждено — ни докстринг `_money` (`roll.py:296-302`), ни
`finance_core/README.md` границы не называют; записи о точности `_money` в README
нет.

**3. Расхождение линта: версия инструмента, а не дефект KB** (инфраструктура) —
Было: ревью тикета 05 (N5) дало «14 ERR I7» (`2 × «таблица inventory:*
рассинхронизирована» + 12 × «секция ## /… без файла .omp/commands/….md»`) и
вывело, что «линт чист» в приёмке тикетов 01–04 неверен.
Надо (на будущее, одной строкой): гонять линт копией, установленной в ворктри —
`python3 .omp/plugins/node_modules/ontoship/skills/kb-search/gitmark.py lint` —
а не произвольной найденной в системе; при расхождении счётов первым делом
сверять версию инструмента.
Источник: ревью тикета 05, N5.
Статус на main: закрыто объяснением — «линт чист» в приёмке тикетов 01–04 был
ВЕРЕН: канонический линт проекта (ontoship 0.4.8, `.omp/plugins/node_modules/…`,
байт-в-байт = копия из `~/.omp/plugins/cache/…/0.4.8/…`) даёт «✓ чисто (75
файлов), 0 ERR / 0 WARN», `gitmark inventory --check` — «реестр синхронен
(13 команд · 12 навыков)»; 14 ERR I7 приходят из **gitmark 0.1.0**
(`/home/prog7/home/dev/personal/ai-agents-rules/oh-my-pi/omp-1c/skills/kb-search/gitmark.py`)
— чужой старой версии инструмента из другого репозитория, её правила к KB этого
проекта не относятся (та же 14 и на базе 0f4e1b6, и в ворктри). Проверка в
приёмке: обе копии прогнаны, счёт каждой назван.

## Уже закрыто

N1 (горизонт стал концом окна): названо и покрыто — докстринг `Base`/`baseline`
(`actions.py:118-121,131,133-135`), `CONTEXT.md:264`, `README.md:409,434`, тест
`tests/test_actions.py::test_the_window_bounds_the_horizon_and_the_price_line` (приёмка 01, п.6) ·
N2 (`_last_payment` учитывает правки): тест
`tests/test_roll.py::test_a_postponed_payment_extends_the_window_and_never_shortens_it`
(позже → окно 5, раньше → не сокращает) ·
N3 (`_result`: `window` без дефолта): `roll.py:929` — `window: Window` обязателен ·
N4 (сигнатура `roll_window`): `roll.py:524-526` — продолжение выровнено по `(` (16 пробелов, как у соседних) ·
N9 (`window=` второй путь): выбран путь «записать риск» — инвариант записан в
`README.md:334-336`, `roll.py:577-581`, `roll.py:1015-1016`, `CONTEXT.md:261-263` ·
N5 (докстринг `DealRoll.freedom`): `roll.py:161-162` — «за окно долги не закрылись» ·
N7 («окно не продлевают»): `CONTEXT.md:257-260` и `README.md:320-323` — «границы по долгам не даёт вовсе … предел — доходы и `max_months`» ·
N8 (глоссарий запрещал «горизонт»): `CONTEXT.md:264` — запрещены «период, срок проката», «горизонт» назван термином кода (дата конца окна, `horizon()`) ·
Бывший Код-2 (стоимость второго прохода × итерации сходимости; был условный): закрыт
свидетелем тикета 04 — проход по графику считается ОДИН раз на прогон
(`_roll_pass=1`, `_graph_pass=1` на окне 240 мес; на прежнем коде того же
сценария 14 вызовов `_roll_pass` = 7 итераций × 2), сценарий ревью 02 (окно
600, долг 300 000 @36 %/год + доходы 2 000/мес): `roll_months` 2,9029 → 0,1059 с,
вызовов `_roll_pass` за прогон 56 → 1, стоимость прохода 0,0196 с и не
повторяется; записано в `docs/decisions/month-by-month-convergence.md` ·
Бывший Код-6 (цессия участника копилки внутри окна; был условный): закрыт
свидетелем тикета 07 — цель единицы идёт 100 000 → 100 000 → 120 000 (дельта
передачи проведена своим месяцем), котлы 1 000/2 000/3 000, остаток проката
равен канону на все три месяца, 31.03 = 139 239,04 в обоих носителях; мутация
«цессия участника не проводится» падает ровно на
`tests/test_roll.py::test_an_inside_window_assignment_enters_the_member_balance_and_the_target`,
снимок `targets` в `_DealsEntry` закреплён отдельным тестом после блокирующего
замечания B1 ревью 07 (свидетель — ворктри ld-07; в main тикет 07 на момент
записи ещё не слит, шипмент идёт своим ходом).

## Приёмка

- [ ] Код-1: на книге из одних пробелов прогноз больше не показывает «закрыт»
  рядом с «не закрывается», либо первая дата явно помечена как ничего не
  значащая — проверяется прогоном.
- [ ] Код-2: в `tests/test_actions.py` нет неиспользуемого импорта `Payment`;
  прогоны зелёные.
- [ ] Код-3: условие `if since <= when <= until` убрано из построения `by_day`;
  тест с платежами вне отрезка (до `since` и после `until`) показывает, что
  начисления не меняются; при правке прокатной связки — тест и на путь
  `budgets=`/`window=` у `roll_deals`.
- [ ] Код-4: выбор закрыт — либо прокат вычитает строки `rows` с датой < `since`
  (расхождение 0 на обоих сценариях ревью 06), либо заявление «прокат = канон»
  сужено с названным исключением; сверка числом.
- [ ] Код-5: решение по дельте цессии в середине месяца записано (оставить — и
  тогда тест с числом, либо считать пропорционально дням) и закреплено тестом.
- [ ] Код-6: решение по `short` закрытой единицы записано — либо закрытая единица
  перестаёт показывать нехватку (и это закреплено тестом), либо поведение
  объявлено осознанным и названо в доках.
- [ ] Доки-1: `finance_core/README.md` и `CONTEXT.md` не приписывают
  `payoff_by_graph` причину конца окна — сверка со строками кода.
- [ ] Доки-2: решение по границе точности `_money` записано (запись в README либо
  явный отказ от неё).
- [ ] Доки-3: при расхождении счётов линта обе копии прогнаны и счёт каждой
  назван; линт копией из ворктри (`.omp/plugins/node_modules/ontoship/…`)
  чист, версия инструмента сверена.
- [ ] Прогоны (`python3 -m pytest tests/`) зелёные — код-ран; линт KB
  установленной копией чист — доки-ран.
- [x] Бывший Код-2 (был условный) — закрыт свидетелем тикета 04: множитель снят
  (проход один раз на прогон, `_roll_pass` 56 → 1, `roll_months` 2,9029 → 0,1059 с
  на сценарии ревью 02), замер-свидетель и решение — в «Уже закрыто».
- [x] Бывший Код-6 (был условный) — закрыт свидетелем тикета 07: цель единицы
  100 000 → 100 000 → 120 000, остаток проката = канон все три месяца
  (31.03 = 139 239,04), тест-свидетель —
  `tests/test_roll.py::test_an_inside_window_assignment_enters_the_member_balance_and_the_target`;
  детали — в «Уже закрыто».

## Как пополнять

Новый хвост дописывается пунктом в «Код» или «Доки» с источником и чекбоксом
приёмки ДО старта рана.
