---
node_type: ticket
title: Правило даты дыры
service: _platform
status: archived
updated: 2026-09-24
links:
  part_of: [README.md]
  documents: [../../../finance_core/solver.py]
---

# 05: Правило даты дыры

**What to build:** у стартовой дыры — дыры, предшествующей первым событиям, —
записано одно правило, и оба пути его держат: окно известно (`roll_cash`) →
дата равна `window_start` месяца (для первого — `start` проката, для
остальных — 1-е число); окна нет (`run()`) → None: движок не спрашивает
«сегодня» и не выдумает, «когда» началось. Сегодня два теста фиксируют разное
поведение без общего правила: `test_hole_comes_from_the_outside` (None) и
`test_roll_cash_hole_on_starting_negative_balance` (дата окна); README
описывает только `None` для `Result.hole_date`, у `CashMonth.hole_date`
оговорки нет — «правило одно на оба пути» нарушено на уровне формулировок.

**Blocked by:** —

## Приёмка

- [x] Правило записано: `finance_core/README.md` (секции кассы) и докстринги
      `Result.hole_date`/`CashMonth.hole_date` говорят одно
- [x] `CONTEXT.md` («Дыра») не противоречит правилу: дата отвечает на «когда
      началась», а не на «есть ли»
- [x] Оба существующих теста стартовой дыры остались зелёными и ссылаются на
      одно правило (комментарий/докстринг)
- [x] `finance_core/README.md` и `CONTEXT.md` согласованы между собой
- [ ] Синтетические тесты движка зелёные; сценарные тесты потребителя зелёные
  — движок 231 зелёный (230 база + 1 новый пин: стартовая дыра месяца ≥ 2
  датируется 1-м числом); в `../finance` против worktree 45/48: те же 3
  пред-существующих фейла базовой линии (два пина тикета 02, приборная панель
  `test_formats`), новых отказов нет; линт KB чист; промежуточный слом
  `CashMonth.hole` пойман синтетикой до стоп-пункта
