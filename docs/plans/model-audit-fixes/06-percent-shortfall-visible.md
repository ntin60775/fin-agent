---
node_type: ticket
title: Урезанный процентный платёж виден
service: _platform
status: active
updated: 2026-09-24
links:
  part_of: [README.md]
  documents: [../../../finance_core/roll.py]
---

# 06: Урезанный процентный платёж виден

**What to build:** обязательный платёж процентом от остатка, которому не хватило
пула месяца, называется числом. Бюджет проката фиксируется на старте
(задокументировано: «в бюджете она считается от остатка на начало проката, а в
платеже — от остатка на начало месяца»), а `want` считается уже с начисленными
процентами: когда `want > pool`, платёж урезается через `min(want, room, pool)`,
и недоплата растворяется — у `DealMonth` нет ни суммы `want`, ни недоплаты, ни
месяца, вхождение процентом не имеет `remaining`, аррериса нигде нет.

Воспроизведение (синтетика, проверено на `main`): заём 10 000 под 36 %/год,
`percent=0.1`, приходов нет, `monthly_extra=0` → месяц 1: `interest` 300,
`want` = 10 % × 10 300 = 1 030, платёж 1 000 — недоплата 30 не видна ни в одном
выходе; при остатке, растущем быстрее графика (ставка выше), недоплата
повторяется и растёт.

Контракт: **видимость, не пересчёт** — baseline остаётся зафиксированным на
старте; недоплата названа числом: поле `DealMonth.short: Decimal` — сумма
`want − amount` по обязательным вхождениям месяца, виден общий случай
урезания `want > amount` (по пулу, остатку или котлу), не только процентный.
Аррерис между месяцами не накапливается: `paid`, `free` и пул месяца не
меняются. Описывается в `finance_core/README.md`.

**Blocked by:** —

## Приёмка

- [x] Синтетический тест воспроизведения: месяц 1 из примера даёт видимую
      недоплату 30 — сумма читается из `DealMonth.short`, а не отдельным
      пересчётом снаружи
- [x] Общий случай урезания виден: `want > amount` по пулу, остатку или котлу
      попадает в `short`; месяцы без урезания `short == 0`
- [x] Месяцы, где пул покрывает `want`, не изменились: `paid` прежний
- [x] `finance_core/README.md`: сказано, что урезанный обязательный платёж
      виден и чем именно
- [ ] Синтетические тесты движка зелёные; сценарные тесты потребителя зелёные
  — движок 236 зелёный (231 база + 5 новых: воспроизведение недоплаты 30,
  ноль без урезания, вырез по остатку, вырез по котлу, граница
  отрицательного пула); в `../finance`
  против worktree 45/48: те же 3 пред-существующих фейла базовой линии
  тикета 05 (два пина тикета 02, приборная панель `test_formats`), новых
  отказов нет; линт KB чист
