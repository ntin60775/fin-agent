---
node_type: ticket
title: Единый базис ликвидности для floor gap
service: _platform
status: archived
updated: 2026-09-23
links:
  part_of: [README.md]
  depends_on: [02-floor-gap-consumed.md, 03-floor-at-window-start.md]
  documents: [../../../finance_core/solver.py]
---

# 04: Единый базис ликвидности для floor gap

**What to build:** `floor_gap` считается от доступной ликвидности всех
кошельков — того же базиса, что у дыры (`_money_total`) и свободных денег, —
а не только от основного кошелька. Сегодня деньги, лежащие на другом кошельке,
дают в одном месяце `free > 0` и `floor_gap > 0` одновременно: свободные деньги
их видят, а прожить на них floor не разрешает; перевод ровно то средство,
которым уже лечится необеспеченность платежа. Базис — `wallet_money` по
доступным кошелькам: свободный лимит кредитного деньгами не считается,
недоступный не участвует (правило уже живёт в `model.wallet_money` и в
`_money_total`).

Смене базиса сопутствуют точки оценки — зеркало дыры: начало месяца (из
тикета 03) плюс каждое обязательное событие; в `roll_cash` — до досрочек
(досрочка идёт после отложенного на жизнь), в `run()` — каждое событие линии.
Иначе агрегатный остаток меряется только на шагах основного кошелька и провал
от платежа с другого кошелька не виден — базис меняется впустую. Вычет
`_unpaid` (обещанного) из floor_gap не входит: остаток остаётся сырым,
обещанное видно в `unsecured`/`free`; если понадобится — отдельный тикет.

Контракт фиксируется в `finance_core/README.md` и `CONTEXT.md`: «остаток» в
формуле floor gap — совокупная доступная ликвидность; `min_balance` и
`end_balance` остаются метриками основного кошелька — это разные величины, и
названы они по-разному.

**Blocked by:** 02, 03 — та же функция `_assess_floor`: сначала прожитое и старт
окна, потом базис, чтобы оценку не переписывать дважды.

## Приёмка

- [x] Синтетика «деньги на другом кошельке»: когда суммарной доступной
      ликвидности хватает до прихода, `floor_gap` не показывает нехватку
- [x] Недоступный кошелёк и свободный лимит в базис не входят (`wallet_money`)
- [x] Точки оценки — зеркало дыры: начало месяца и каждое обязательное
      событие; в `roll_cash` — до досрочек
- [x] Обещанное (`_unpaid`) из floor_gap не вычитается — остаток сырой
- [x] `min_balance`/`end_balance` не изменились — остаются метриками основного кошелька
- [x] `finance_core/README.md` и `CONTEXT.md` согласованы с базисом
- [ ] Синтетические тесты движка зелёные; сценарные тесты потребителя зелёные
  — движок 230 зелёный (223 база + 7 новых воспроизведений); в `../finance`
  против worktree (`FIN_AGENT_HOME`) 45/48: те же 3 пред-существующих фейла
  базовой линии (два пина тикета 02 — 11 201,03 против 21 891,83, значение
  движка не изменилось; приборная панель `test_formats`), новых отказов нет;
  линт KB чист; пересчёт пинов — в финансовой зоне отдельным ходом
