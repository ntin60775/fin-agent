---
node_type: runbook
title: Публикация архитектурной зоны в открытый репозиторий
service: _platform
status: active
updated: 2026-09-20
links:
  depends_on: [../decisions/zone-split.md]
---

# Публикация в открытый репозиторий

Зона спроектирована так, чтобы её **можно было публиковать**: личных данных в ней
нет — это и было причиной разделения ([zone-split.md](../decisions/zone-split.md)).
Технически ничто не мешает: хука, блокирующего `git push`, здесь нет — он живёт в
финансовой зоне.

## Что проверено

| Проверка | Результат |
|---|---|
| Личные числа, имена кредиторов, банки, номера счетов | нет (суммы без `₽` вычищены 2026-09-15 — см. п. 1) |
| История коммитов | вычищена (перезапись): следов нет, хеши после неё изменились |
| Абсолютные пути `/home/…` в отслеживаемых файлах | нет |
| Хук, блокирующий push | отсутствует (`.omp/extensions/` нет) |
| `.gitignore` закрывает производное | `.gitmark/`, `*-map.html`, `.scratch/`, `__pycache__/` |
| Ветка | `main` |
| Объём | 63 отслеживаемых файла, ≈0,67 МБ (682 514 байт) |

## Факт публикации

Репозиторий опубликован: `origin` = `git@github.com:ntin60775/fin-agent.git`,
`main` в синхроне с `origin/main`.

## Как подготовить к публикации

1. **История.** Правка рабочего дерева историю **не чистит**. Проверить:

   ```bash
   git log -p --all | grep -nE '[0-9] ?(₽|руб)|…[0-9]{4}'    # суммы со знаком валюты
   git log -p --all | grep -nE '[0-9]+,[0-9]{2}\b|−[0-9]+'   # суммы без знака валюты
   ```

   Второй шаблон обязателен: суммы в текстах стоят и без `₽` — так утечка и
   проскочила, её нашли только аудитом KB 2026-09-15. Плюс **глазами** — на имена
   кредиторов и названия банков: шаблоном их не поймать, а в заявках они
   появляются легко. Найдётся — переписать историю **до публикации**. После первого
   push это уже невозможно: форки и кэши не отзываются.

2. **Лицензия.** Собственный код (`finance_core/`, тесты, документация) — **MIT**:
   [LICENSE](../../LICENSE). `.omp/` и `docs/ontology.md` — пакет **OntoShip** (MIT,
   © Valera Kovalskii): [LICENSES/ontoship-MIT.txt](../../LICENSES/ontoship-MIT.txt),
   авторство указано в `README.md`.

3. **README.md** — есть: что это, что умеет, принципы, происхождение.

## Как опубликовать

```bash
cd fin-agent
git remote add origin git@github.com:<owner>/<repo>.git   # SSH, не HTTPS
git push -u origin HEAD
```

Если `origin` уже существует, `git remote add origin` падает — используйте
`git remote set-url origin git@github.com:<owner>/<repo>.git` или пропустите
шаг, если адрес верный.

**Из финансовой зоны push невозможен.** Её хук блокирует `git push` по шаблону
команды — в том числе нацеленный на этот репозиторий. Публикация идёт только из
сессии, открытой здесь; это не запрет, а страховка.

## Обратная сторона

Опубликованный репозиторий **публичен навсегда**: история, форки, кэши поисковиков.
Если позже понадобится закрыть — удаление не отменяет копий. Поэтому пункт 1
делается до, а не после.
