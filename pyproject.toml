[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "finance-core"
version = "0.1.0"
description = "Ядро расчёта личных финансов: касса и долги"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["finance_core"]
